// Contiene todo dentro de una sola linea 
// For tambien es loops, pero a diferencia de while, for se compone de 3 expresiones inicializacion; comparacion; terminariteracion

/* let i = 0;
while (i < 10) {
    if (i % 2 == 0) {
        console.log('Numero par', i); // Imprimi el valor de i 
    }
    i++; // Incrementa el valor de i en 1 
} */

/* for (inicializacion; comparacion; terminariteracion) {
    codigo del if 
} */

for (let i = 2; i < 10; i++) {
    if (i % 2 == 0 ) {
        console.log('Numero par' , i);
    }
}